Value Filldown Port  (\d+/\d+)
Value RxName   (\w+.*\w+)
Value RxValue   (\d+|(n/a))
Value TxName   (\w+.*\w+)
Value TxValue   (\d+|(n/a))

Start
  ^Counters for port ${Port}:
  ^RX ${RxName} \s+ ${RxValue} \| TX ${TxName} \s+ ${TxValue} -> Record
  ^RX ${RxName} \s+ ${RxValue} \| -> Record